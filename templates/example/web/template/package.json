{
  "name": "{{.project_name}}",
  "version": "1.0.0",
  "description": "{{.description}}",
  "author": "{{.author}}",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"{{if .eslint}},
    "lint": "eslint src --ext .js,.jsx,.ts,.tsx"{{end}}{{if .prettier}},
    "format": "prettier --write src/**/*"{{end}}
  },
  "dependencies": {},
  "devDependencies": {
    "vite": "^5.0.0"{{if .typescript}},
    "typescript": "^5.3.0"{{end}}{{if .eslint}},
    "eslint": "^8.0.0"{{end}}{{if .prettier}},
    "prettier": "^3.0.0"{{end}}
  }
}
